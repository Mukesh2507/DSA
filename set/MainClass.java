package com.set;




// work in  constant time in set ->implements->class->Hashset,LinkedHashSet,EnumSet,TreeSet
// methods of set-> add(element),addAll(Collection),remove(element),removeAll().retainAll(),retain()
//clear(),size(),conatins(),conatainsAll(),isEmpty(),toArray()

import java.util.Arrays;
import java.util.Collections;
import java.util.HashSet;
import java.util.Set;

//provide Mathemetical  set features and does not contain duplicate


//union->x.addAll(y);
//intersection->x.retainAll(y);
//subset-> x.containsAll(y)
public class MainClass{

    public static void main(String[]args){


        Set x = new HashSet();
        x.add(23);
        x.add(24);


        Set y = new HashSet();


        y.add(7);
        y.add(8);









    }



}
